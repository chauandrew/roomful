import type { GameMeta } from "@/lib/types";

export const sketchChainMeta: GameMeta = {
  id: "sketch-chain",
  name: "Sketch Chain",
  description:
    "Telephone with drawings: write a phrase, draw the phrase you're handed, describe the drawing you're handed… then watch every chain fall apart on the big screen.",
  mode: "multi-user",
  minPlayers: 2,
  maxPlayers: 10,
  durationMinutes: 20,
  accent: "#7a5cbf",
};
