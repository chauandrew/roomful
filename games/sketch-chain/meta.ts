import type { GameMeta } from "@/lib/types";

export const sketchChainMeta: GameMeta = {
  id: "sketch-chain",
  name: "Sketch Chain",
  description: "Telephone, but everyone draws. Watch it fall apart on the big screen.",
  mode: "multi-user",
  minPlayers: 2,
  maxPlayers: 10,
  durationMinutes: 20,
  accent: "#7a5cbf",
};
