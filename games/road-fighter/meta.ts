import type { GameMeta } from "@/lib/types";

export const roadFighterMeta: GameMeta = {
  id: "road-fighter",
  name: "Road Fighter",
  description: "Street Fighter IRL. Throw the pose, not the punch.",
  mode: "single-device",
  minPlayers: 2,
  maxPlayers: 2,
  durationMinutes: 3,
  accent: "#fb923c",
};
