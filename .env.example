# Where the browser finds the realtime room server.
# Leave unset for local dev (defaults to 127.0.0.1:1999, where `wrangler dev` runs).
# After `npm run deploy:party`, set this to your deployed Cloudflare Worker, e.g.:
# NEXT_PUBLIC_PARTYKIT_HOST=roomful.<your-subdomain>.workers.dev
