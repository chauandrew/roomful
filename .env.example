# Where the browser finds the PartyKit realtime server.
# Leave unset for local dev (defaults to 127.0.0.1:1999, where `partykit dev` runs).
# After `npm run deploy:party`, set this to your deployed party host, e.g.:
# NEXT_PUBLIC_PARTYKIT_HOST=roomful.<your-partykit-username>.partykit.dev
